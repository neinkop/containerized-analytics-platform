# containerized-analytics-platform

Python Environment: 
python3 -m venv venv
source venv/bin/activate
python3 -m pip install pandas pyarrow flask waitress statsmodels holidays
python3 -m pip install pandas pyarrow flask <PACKAGE>